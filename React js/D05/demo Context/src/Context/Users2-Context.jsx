import { createContext } from "react";

export const UsersContext2 = createContext({userData:[],setUserData:()=>{}, disabled:false, addNewUser:()=>{}})

// export const langContext = createContext([{lang:''}])